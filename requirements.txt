# requirements.txt

# Core web server
Flask==2.3.3
gunicorn==21.2.0

# File parsing
pdfplumber==0.10.3
EbookLib==0.18
beautifulsoup4==4.12.3

# --- NEW TTS LIBRARY ---
# Coqui-TTS is a much better-packaged library for production use
TTS==0.22.0

# Pin numpy for compatibility with PyTorch (a dependency of TTS)
numpy==1.26.4