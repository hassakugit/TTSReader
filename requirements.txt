# requirements.txt

# Core web server
Flask==2.3.3
gunicorn==21.2.0

# File parsing
pdfplumber==0.10.3
EbookLib==0.18
beautifulsoup4==4.12.3

# --- Coqui-TTS and its specific dependencies ---
# TTS library itself
TTS==0.22.0

# Pinning numpy to the *exact* version required by TTS==0.22.0 on Python 3.10
# This resolves the dependency conflict.
numpy==1.22.0